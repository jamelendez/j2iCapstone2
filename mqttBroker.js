
//const mongoose = require('mongoose')
//const config = require('config');
//const db = config.get('mongoURI');

// Connect to Mongo
/*mongoose.set('useFindAndModify', false);
mongoose
    .connect(db, {
        useNewUrlParser: true,
        useUnifiedTopology: true,
        useCreateIndex: true
    })
    .then(() => console.log('MongoDB Connected to MQTT Server...'))
    .catch(err => console.log(err));*/

//const DIChannels = require('./models/DIChannels');












// MQTT STUFF

//var testTopic = "/cc3200/Meliora/test"
//var toggleMsg = "Toggle the state of RED LED"
// var msg;
// var options = {
//     retain: true,
//     qos: 1
// }









/*// Subscribe to topic
mqttClient.subscribe(testTopic, { qos: 1 });

// Recieve message
mqttClient.on('message', function (topic, message, packet) {
    console.log("message is " + message);
    console.log("topic is " + topic);
});*/