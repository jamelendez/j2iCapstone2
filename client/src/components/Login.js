import React, { Component } from 'react';
import { Button, Form, FormGroup, Input, Label } from 'reactstrap';

class Login extends Component {
    render() {
        return (
            <div>
                <h1>Log In</h1>
                
                <Button style={{marginTop: '2rem'}}>Log In</Button>

            </div>
        )
    }
}

export default Login; 